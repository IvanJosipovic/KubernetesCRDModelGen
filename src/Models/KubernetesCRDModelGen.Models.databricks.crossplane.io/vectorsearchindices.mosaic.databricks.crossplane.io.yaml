apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vectorsearchindices.mosaic.databricks.crossplane.io
spec:
  group: mosaic.databricks.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: VectorSearchIndex
    listKind: VectorSearchIndexList
    plural: vectorsearchindices
    singular: vectorsearchindex
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VectorSearchIndex is the Schema for the VectorSearchIndexs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VectorSearchIndexSpec defines the desired state of VectorSearchIndex
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  deltaSyncIndexSpec:
                    description: (object) Specification for Delta Sync Index. Required if index_type is DELTA_SYNC. This field is a block and is documented below.
                    items:
                      properties:
                        embeddingSourceColumns:
                          description: 'array of objects representing columns that contain the embedding source.  Each entry consists of:'
                          items:
                            properties:
                              embeddingModelEndpointName:
                                description: The name of the embedding model endpoint, used by default for both ingestion and querying.
                                type: string
                              embeddingModelEndpointNameRef:
                                description: Reference to a ModelServing in serving to populate embeddingModelEndpointName.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              embeddingModelEndpointNameSelector:
                                description: Selector for a ModelServing in serving to populate embeddingModelEndpointName.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with matching labels is selected.
                                    type: object
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              modelEndpointNameForQuery:
                                description: The name of the embedding model endpoint which, if specified, is used for querying (not ingestion).
                                type: string
                              name:
                                description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                                type: string
                            type: object
                          type: array
                        embeddingVectorColumns:
                          description: 'array of objects representing columns that contain the embedding vectors. Each entry consists of:'
                          items:
                            properties:
                              embeddingDimension:
                                description: Dimension of the embedding vector.
                                type: number
                              name:
                                description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                                type: string
                            type: object
                          type: array
                        embeddingWritebackTable:
                          description: Automatically sync the vector index contents and computed embeddings to the specified Delta table. The only supported table name is the index name with the suffix _writeback_table.
                          type: string
                        pipelineType:
                          description: 'Pipeline execution mode. Possible values are:'
                          type: string
                        sourceTable:
                          description: The name of the source table.
                          type: string
                      type: object
                    type: array
                  directAccessIndexSpec:
                    description: (object) Specification for Direct Vector Access Index. Required if index_type is DIRECT_ACCESS. This field is a block and is documented below.
                    items:
                      properties:
                        embeddingSourceColumns:
                          description: 'array of objects representing columns that contain the embedding source.  Each entry consists of:'
                          items:
                            properties:
                              embeddingModelEndpointName:
                                description: The name of the embedding model endpoint, used by default for both ingestion and querying.
                                type: string
                              modelEndpointNameForQuery:
                                description: The name of the embedding model endpoint which, if specified, is used for querying (not ingestion).
                                type: string
                              name:
                                description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                                type: string
                            type: object
                          type: array
                        embeddingVectorColumns:
                          description: 'array of objects representing columns that contain the embedding vectors. Each entry consists of:'
                          items:
                            properties:
                              embeddingDimension:
                                description: Dimension of the embedding vector.
                                type: number
                              name:
                                description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                                type: string
                            type: object
                          type: array
                        schemaJson:
                          description: The schema of the index in JSON format.  Check the API documentation for a list of supported data types.
                          type: string
                      type: object
                    type: array
                  endpointName:
                    description: The name of the Mosaic AI Vector Search Endpoint that will be used for indexing the data.
                    type: string
                  endpointNameRef:
                    description: Reference to a VectorSearchEndpoint in mosaic to populate endpointName.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  endpointNameSelector:
                    description: Selector for a VectorSearchEndpoint in mosaic to populate endpointName.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  indexType:
                    description: 'Mosaic AI Vector Search index type. Currently supported values are:'
                    type: string
                  name:
                    description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                    type: string
                  primaryKey:
                    description: The column name that will be used as a primary key.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  deltaSyncIndexSpec:
                    description: (object) Specification for Delta Sync Index. Required if index_type is DELTA_SYNC. This field is a block and is documented below.
                    items:
                      properties:
                        embeddingSourceColumns:
                          description: 'array of objects representing columns that contain the embedding source.  Each entry consists of:'
                          items:
                            properties:
                              embeddingModelEndpointName:
                                description: The name of the embedding model endpoint, used by default for both ingestion and querying.
                                type: string
                              embeddingModelEndpointNameRef:
                                description: Reference to a ModelServing in serving to populate embeddingModelEndpointName.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              embeddingModelEndpointNameSelector:
                                description: Selector for a ModelServing in serving to populate embeddingModelEndpointName.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with matching labels is selected.
                                    type: object
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              modelEndpointNameForQuery:
                                description: The name of the embedding model endpoint which, if specified, is used for querying (not ingestion).
                                type: string
                              name:
                                description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                                type: string
                            type: object
                          type: array
                        embeddingVectorColumns:
                          description: 'array of objects representing columns that contain the embedding vectors. Each entry consists of:'
                          items:
                            properties:
                              embeddingDimension:
                                description: Dimension of the embedding vector.
                                type: number
                              name:
                                description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                                type: string
                            type: object
                          type: array
                        embeddingWritebackTable:
                          description: Automatically sync the vector index contents and computed embeddings to the specified Delta table. The only supported table name is the index name with the suffix _writeback_table.
                          type: string
                        pipelineType:
                          description: 'Pipeline execution mode. Possible values are:'
                          type: string
                        sourceTable:
                          description: The name of the source table.
                          type: string
                      type: object
                    type: array
                  directAccessIndexSpec:
                    description: (object) Specification for Direct Vector Access Index. Required if index_type is DIRECT_ACCESS. This field is a block and is documented below.
                    items:
                      properties:
                        embeddingSourceColumns:
                          description: 'array of objects representing columns that contain the embedding source.  Each entry consists of:'
                          items:
                            properties:
                              embeddingModelEndpointName:
                                description: The name of the embedding model endpoint, used by default for both ingestion and querying.
                                type: string
                              modelEndpointNameForQuery:
                                description: The name of the embedding model endpoint which, if specified, is used for querying (not ingestion).
                                type: string
                              name:
                                description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                                type: string
                            type: object
                          type: array
                        embeddingVectorColumns:
                          description: 'array of objects representing columns that contain the embedding vectors. Each entry consists of:'
                          items:
                            properties:
                              embeddingDimension:
                                description: Dimension of the embedding vector.
                                type: number
                              name:
                                description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                                type: string
                            type: object
                          type: array
                        schemaJson:
                          description: The schema of the index in JSON format.  Check the API documentation for a list of supported data types.
                          type: string
                      type: object
                    type: array
                  endpointName:
                    description: The name of the Mosaic AI Vector Search Endpoint that will be used for indexing the data.
                    type: string
                  endpointNameRef:
                    description: Reference to a VectorSearchEndpoint in mosaic to populate endpointName.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  endpointNameSelector:
                    description: Selector for a VectorSearchEndpoint in mosaic to populate endpointName.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  indexType:
                    description: 'Mosaic AI Vector Search index type. Currently supported values are:'
                    type: string
                  name:
                    description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                    type: string
                  primaryKey:
                    description: The column name that will be used as a primary key.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.indexType is a required parameter
              rule: "!('*' in self.managementPolicies || 'Create' in self.managementPolicies || 'Update' in self.managementPolicies) || has(self.forProvider.indexType) || (has(self.initProvider) && has(self.initProvider.indexType))"
            - message: spec.forProvider.name is a required parameter
              rule: "!('*' in self.managementPolicies || 'Create' in self.managementPolicies || 'Update' in self.managementPolicies) || has(self.forProvider.name) || (has(self.initProvider) && has(self.initProvider.name))"
            - message: spec.forProvider.primaryKey is a required parameter
              rule: "!('*' in self.managementPolicies || 'Create' in self.managementPolicies || 'Update' in self.managementPolicies) || has(self.forProvider.primaryKey) || (has(self.initProvider) && has(self.initProvider.primaryKey))"
          status:
            description: VectorSearchIndexStatus defines the observed state of VectorSearchIndex.
            properties:
              atProvider:
                properties:
                  creator:
                    description: Creator of the endpoint.
                    type: string
                  deltaSyncIndexSpec:
                    description: (object) Specification for Delta Sync Index. Required if index_type is DELTA_SYNC. This field is a block and is documented below.
                    items:
                      properties:
                        embeddingSourceColumns:
                          description: 'array of objects representing columns that contain the embedding source.  Each entry consists of:'
                          items:
                            properties:
                              embeddingModelEndpointName:
                                description: The name of the embedding model endpoint, used by default for both ingestion and querying.
                                type: string
                              modelEndpointNameForQuery:
                                description: The name of the embedding model endpoint which, if specified, is used for querying (not ingestion).
                                type: string
                              name:
                                description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                                type: string
                            type: object
                          type: array
                        embeddingVectorColumns:
                          description: 'array of objects representing columns that contain the embedding vectors. Each entry consists of:'
                          items:
                            properties:
                              embeddingDimension:
                                description: Dimension of the embedding vector.
                                type: number
                              name:
                                description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                                type: string
                            type: object
                          type: array
                        embeddingWritebackTable:
                          description: Automatically sync the vector index contents and computed embeddings to the specified Delta table. The only supported table name is the index name with the suffix _writeback_table.
                          type: string
                        pipelineId:
                          description: ID of the associated Delta Live Table pipeline.
                          type: string
                        pipelineType:
                          description: 'Pipeline execution mode. Possible values are:'
                          type: string
                        sourceTable:
                          description: The name of the source table.
                          type: string
                      type: object
                    type: array
                  directAccessIndexSpec:
                    description: (object) Specification for Direct Vector Access Index. Required if index_type is DIRECT_ACCESS. This field is a block and is documented below.
                    items:
                      properties:
                        embeddingSourceColumns:
                          description: 'array of objects representing columns that contain the embedding source.  Each entry consists of:'
                          items:
                            properties:
                              embeddingModelEndpointName:
                                description: The name of the embedding model endpoint, used by default for both ingestion and querying.
                                type: string
                              modelEndpointNameForQuery:
                                description: The name of the embedding model endpoint which, if specified, is used for querying (not ingestion).
                                type: string
                              name:
                                description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                                type: string
                            type: object
                          type: array
                        embeddingVectorColumns:
                          description: 'array of objects representing columns that contain the embedding vectors. Each entry consists of:'
                          items:
                            properties:
                              embeddingDimension:
                                description: Dimension of the embedding vector.
                                type: number
                              name:
                                description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                                type: string
                            type: object
                          type: array
                        schemaJson:
                          description: The schema of the index in JSON format.  Check the API documentation for a list of supported data types.
                          type: string
                      type: object
                    type: array
                  endpointName:
                    description: The name of the Mosaic AI Vector Search Endpoint that will be used for indexing the data.
                    type: string
                  id:
                    description: The same as the name of the index.
                    type: string
                  indexType:
                    description: 'Mosaic AI Vector Search index type. Currently supported values are:'
                    type: string
                  name:
                    description: Three-level name of the Mosaic AI Vector Search Index to create (catalog.schema.index_name).
                    type: string
                  primaryKey:
                    description: The column name that will be used as a primary key.
                    type: string
                  status:
                    description: 'Object describing the current status of the index consisting of the following fields:'
                    items:
                      properties:
                        indexUrl:
                          description: Index API Url to be used to perform operations on the index
                          type: string
                        indexedRowCount:
                          description: Number of rows indexed
                          type: number
                        message:
                          description: Message associated with the index status
                          type: string
                        ready:
                          description: Whether the index is ready for search
                          type: boolean
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True, False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}